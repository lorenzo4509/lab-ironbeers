<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Beers - My Beer App</title>
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
  <h1>All Beers</h1>
  <div class="beer-list">
    {{#each beers}}
    <div class="beer">
      <img src="{{this.image_url}}" alt="{{this.name}}">
      <h2>{{this.name}}</h2>
      <p>{{this.description}}</p>
      <p>{{this.tagline}}</p>
    </div>
    {{/each}}
  </div>
</body>
</html>
